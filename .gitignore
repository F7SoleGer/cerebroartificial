# local state — never commit (mas .claude/skills/ é versionado)
.claude/*
!.claude/skills/
.DS_Store
node_modules/

# Supabase CLI local state (project ref is committed in supabase/config.toml)
supabase/.temp/
supabase/.branches/
.env
.env.local
